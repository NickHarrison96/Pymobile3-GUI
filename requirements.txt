# Pymobile3-GUI Requirements
# Core GUI framework
PySide6>=6.6.0

# iOS device communication
# Pinned to the 10.x CLI surface this app is written against. The command names
# drift between majors (e.g. `lockdown get-value DeveloperModeStatus` became
# `amfi developer-mode-status`), and those breakages are silent at import time.
pymobiledevice3>=10.0,<11

# System utilities
psutil>=5.9.0

# Async compatibility for AFC service
nest-asyncio>=1.5.0